const AUTH_CONTACT_TYPE = Object.freeze({
  EMAIL: "email",
  PHONE: "phone"
});

const AUTH_CONTACT_TYPE_VALUES = Object.freeze(Object.values(AUTH_CONTACT_TYPE));

module.exports = {
  AUTH_CONTACT_TYPE,
  AUTH_CONTACT_TYPE_VALUES
};
